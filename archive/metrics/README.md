# Metrics Archive

This folder contains historical metrics utilities that are not part of the active runtime.

- `csv_metrics.py`: legacy file-based metrics handler kept for reference.

Active runtime metrics persistence uses SQL-backed handlers under `src/storage/sql_metrics.py`.
