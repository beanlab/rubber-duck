## Purpose

`archive/prompts` stores historical prompt assets that are not part of active runtime wiring.

## Boundaries

- Treat files here as reference material only.
- Production prompt configuration should point to active prompt paths outside `archive/` unless intentionally migrating an archived prompt back into runtime.
