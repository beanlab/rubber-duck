Core:
  - Uses at least 3 words
  - Includes a brownie recipe
  - Tells a joke

Stretch 1:
  - Uses the letter e **exactly** 7 times
  - Includes the word "basil" and "sunflower"
  - Does not include a code block